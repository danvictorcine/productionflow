rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read; // Permite a leitura pública de todos os arquivos
      allow write: if request.auth != null; // Apenas usuários autenticados podem escrever
    }
  }
}
