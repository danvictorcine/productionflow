'use client';
// This file is intentionally left blank to revert to a previous state.
// The new "About Page" editor has been removed to restore application stability.
// To re-enable a dedicated editor, this file would need to be re-implemented
// and the corresponding routing in /admin/pages/page.tsx updated.

export default function RevertedAboutPage() {
  return null;
}
